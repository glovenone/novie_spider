import requests
from scrapy import cmdline

# cmdline.execute("scrapy crawl hunhun520".split())
# cmdline.execute("scrapy crawl zhuishuRank".split())

# cmdline.execute("scrapy crawl video".split())

# baseurl = 'http://54.238.207.228:6800/'
# delProUrl = baseurl + 'delproject.json'
# dictdata = {"project": 'NovelSpiderProject'}
# r = requests.post(delProUrl, data=dictdata)
# print('6.2.delproject : [%s]\n\n' % r.text)
